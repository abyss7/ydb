library("api") {
    public_deps = [ ":protos" ]
}

protobuf_library("protos") {
    sources = [
        "protos/graph.proto",
    ]
}

protobuf_library("shard_protos") {
    deps = [
        "//ydb/core/protos",
    ]

    sources = [
        "shard/protos/counters_shard.proto",
    ]
}
