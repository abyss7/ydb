library("tx_allocator") {
  public_deps = [
    "//ydb/core/base",
    # "//ydb/core/protos",
  ]

  deps = [
    "//ydb/core/engine:minikql",
    "//ydb/core/tablet",
    "//ydb/core/tablet_flat",
    # "//ydb/library/services",
  ]

  sources = [
    "txallocator.cpp",
    "txallocator__reserve.cpp",
    "txallocator__scheme.cpp",
    "txallocator_impl.cpp",
  ]
}
