# yamake2gn: keep  -- hand-written, do not regenerate
#
# Compiled inside //ydb/public/lib/ydb_cli/commands: this workload's commands and
# the topic scenarios in the parent directory need each other, and that target
# is the only user. Kept as a redirect for existing dependants.

group("transfer_workload") {
  public_deps = [ "//ydb/public/lib/ydb_cli/commands" ]
}
