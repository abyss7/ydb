library("ydb") {
  public_deps = [
    # "//ydb/core/grpc_services",
    # ydb_query.cpp:66 ссылается на DoFetchScriptResults — он вынесен из
    # grpc_services в отдельный таргет, чтобы тот не тянул KQP proxy_service.
    "//ydb/core/grpc_services:fetch_script_results",
    # ydb_clickhouse_internal.cpp:54 ссылается на DoReadColumnsRequest — вынесен
    # из grpc_services в отдельный таргет, чтобы тот не тянул sys_view.
    "//ydb/core/grpc_services:read_columns",
    # "//ydb/core/grpc_services/base",
    # "//ydb/library/actors/core",
    # "//ydb/library/grpc/server",
    # "//ydb/public/api/grpc",
    # "//ydb/public/api/grpc:draft",
  ]

  deps = [
    # "//ydb/core/base",
    # "//ydb/core/grpc_services/counters",
    # "//ydb/core/grpc_streaming",
    # "//ydb/public/api/protos",
  ]

  sources = [
    "ydb_clickhouse_internal.cpp",
    "ydb_debug.cpp",
    "ydb_dummy.cpp",
    "ydb_export.cpp",
    "ydb_import.cpp",
    "ydb_logstore.cpp",
    "ydb_object_storage.cpp",
    "ydb_operation.cpp",
    "ydb_query.cpp",
    "ydb_scheme.cpp",
    "ydb_scripting.cpp",
    "ydb_table.cpp",
  ]
}
