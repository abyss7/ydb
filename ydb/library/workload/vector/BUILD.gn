# yamake2gn: keep  -- hand-written, do not regenerate
#
# Compiled inside //ydb/public/lib/ydb_cli/commands: this workload defines CLI
# commands on top of that target's TYdbSimpleCommand / ydb_workload.h, and that
# target is its only user and creates its commands -- see the comment there.
# Kept as a redirect for existing dependants.

group("vector") {
  public_deps = [ "//ydb/public/lib/ydb_cli/commands" ]
}
