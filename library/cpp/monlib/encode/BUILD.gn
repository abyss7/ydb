protobuf_library("protos") {
    sources = [
        "legacy_protobuf/protos/metric_meta.proto",
    ]
}

library("encode") {
    deps = [
        "//library/cpp/monlib/metrics",
    ]

    sources = [
        "encoder.cpp",
        "encoder_state.cpp",
        "format.cpp",
    ]

    serialize_enum_headers = [
        "encoder_state_enum.h",
    ]
}
