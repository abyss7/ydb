config("private_config") {
    defines = [ "USE_ICONV_EXTENSIONS" ]
}

source_set("lite") {
    sources = [
        "ci_string.cpp",
        "codepage.cpp",
        "cp_encrec.cpp",
        "doccodes.cpp",
        "generated/cp_data.cpp",
        "generated/encrec_data.cpp",
    ]
}

library("charset") {
    deps = [
        ":lite",
        "//contrib/libs/libiconv"
    ]

    sources = [
        "decodeunknownplane.cpp",
        "iconv.cpp",
        "wide.cpp",
    ]

    configs = [ ":private_config" ]
}
