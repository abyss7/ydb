# yamake2gn: keep  -- hand-written, do not regenerate

library("lwtrace") {
  public_deps = [
    ":protos",
  ]

  sources = [
    "check.cpp",
    "control.cpp",
    "custom_action.cpp",
    "kill_action.cpp",
    "log_shuttle.cpp",
    "perf.cpp",
    "probes.cpp",
    "shuttle.cpp",
    "sleep_action.cpp",
    "start.cpp",
    "stderr_writer.cpp",
    "symbol.cpp",
    "trace.cpp",
  ]
}

protobuf_library("protos") {
  sources = [ "protos/lwtrace.proto" ]
}
