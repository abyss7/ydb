library("indent_text") {
    sources = [
        "indent_text/indent_text.cpp",
    ]
}

library("quote") {
    sources = [
        "quote/quote.cpp",
    ]
}

library("url") {
    sources =[
        "url/url.cpp",
    ]
}
