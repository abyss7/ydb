config("public_config") {
    include_dirs = [ "include" ]
}

contrib_library("exception") {
    deps = [
        "//contrib/restricted/boost/config",
    ]

    sources = [
        "src/clone_current_exception_non_intrusive.cpp",
    ]

    public_configs = [ ":public_config" ]
}
