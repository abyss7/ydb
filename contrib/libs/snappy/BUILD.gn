config("public_config") {
    include_dirs = [ "include" ]
}

config("private_config") {
    defines = [ "HAVE_CONFIG_H" ]
}

contrib_library("snappy") {
    sources = [
        "snappy-c.cc",
        "snappy-sinksource.cc",
        "snappy-stubs-internal.cc",
        "snappy.cc",
    ]

    public_configs = [ ":public_config" ]
    configs = [ ":private_config" ]
}
